# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.66
tabulate>=0.9
Pillow>=10.0
PyYAML>=6.0

# Deep learning
torch>=2.0
torchvision>=0.15
torchio>=0.18
thop>=0.1.1

# Medical imaging
nibabel>=5.1
SimpleITK>=2.3
antspyx>=0.4
antspynet>=0.2

# Note: dcm2niix is a binary, not a Python package. Install it via:
#   Ubuntu/Debian:  sudo apt-get install -y dcm2niix
#   conda:          conda install -c conda-forge dcm2niix
# It is invoked as a subprocess by src/tafnet/preprocessing/dicom_to_nifti.py.
